- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.config/:
      glob: true
      force: true
      path: config/*

- shell:
  - [git submodule update --init --recursive, Installing submodules]
